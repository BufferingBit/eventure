<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en" class="bg-[#0F172A] text-[#F8FAFC]">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Eventure</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter&family=Poppins:wght@500;700&display=swap"
        rel="stylesheet" />
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }

        h1,
        h2,
        h3 {
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>

<body class="bg-[#0F172A] text-[#F8FAFC]">

    <!-- Navigation Header -->
    <header class="bg-[#1E293B] px-6 py-4 shadow-md flex flex-col md:flex-row justify-between items-center">
        <!-- Logo / Brand -->
        <div class="text-2xl font-bold text-[#3ABEFF] mb-4 md:mb-0">
            <a href="/">Eventure</a>
        </div>

        <!-- Navigation Links -->
        <nav class="flex items-center gap-4">
            <a href="/" class="hover:text-[#B388EB] transition duration-300">Home</a>
            <a href="/user/profile" class="hover:text-[#B388EB] transition duration-300">Profile</a>
        </nav>
    </header>
=======
<header class="bg-gradient-to-r from-indigo-900 to-blue-500 text-white px-8 py-4 flex justify-between items-center shadow">
    <h1 class="text-2xl font-bold"><a href="/">Eventure</a></h1>

    <form method="get" action="/" class="flex w-1/2 max-w-lg">
        <input type="text" name="search" placeholder="Search college or club" value="<%= searchTerm || '' %>"
            class="w-full px-4 py-2 rounded-l-full focus:outline-none text-black" />
        <button type="submit" class="px-4 py-2 bg-white text-blue-500 rounded-r-full font-semibold">Search</button>
    </form>

    <div class="flex items-center gap-4">
        <% if (user) { %>
            <a href="/profile" >
            <div class="flex items-center gap-2">
                <img src="<%= user.photo %>" alt="Profile" class="w-8 h-8 rounded-full">
                <span><%= user.name %></span>
            </div>
        </a>
            <a href="/logout" class="bg-white text-black px-4 py-2 rounded-full hover:bg-gray-100">Logout</a>
        <% } else { %>
            <div class="flex gap-2">
                <a href="/login" class="bg-transparent border-2 border-white text-white px-4 py-2 rounded-full hover:bg-white hover:text-blue-500 transition">Login</a>
                <a href="/signup" class="bg-white text-blue-500 px-4 py-2 rounded-full hover:bg-gray-100">Sign Up</a>
            </div>
        <% } %>
    </div>
</header>
>>>>>>> fresh-start

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>College</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>

<body class="min-h-screen flex flex-col">
    <!-- ✅ Header Include -->
    <%- include('../partials/header.ejs') %>

    <div class="flex-grow px-6 py-4">
        <div class="bg-white shadow p-4 rounded mb-6">
            <p class="text-lg font-semibold">🏫 <%= college.name %></p>
            <p class="text-sm text-gray-600">📍 <%= college.location %></p>
            <button class="mt-2 px-4 py-2 bg-blue-500 text-white rounded">Follow</button>
            <p>Clubs: <%= clubs.length %></p>
        </div>

        <div class="flex gap-4 mb-4">
            <button class="px-4 py-2 bg-gray-200 rounded">Clubs</button>
            <button class="px-4 py-2 bg-gray-200 rounded">Societies</button>
            <button class="px-4 py-2 bg-gray-200 rounded">Fests</button>
        </div>

        <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
            <% for (let i = 1; i <= 10; i++) { %>
                <div class="bg-white p-4 shadow rounded text-center">Club <%= i %></div>
            <% } %>
        </div>
    </div>

    <!-- ✅ Footer Include -->
    <%- include('../partials/footer.ejs') %>
</body>

</html>
